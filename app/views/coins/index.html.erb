<p id="notice"><%= notice %></p>

<h1><%= t('titles.coins') %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('descs.description') %>%</th>
      <th><%= t('descs.acronym') %></th>
      <th><%= t('descs.image') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @coins.each do |coin| %>
      <tr>
        <td><%= coin.description %></td>
        <td><%= coin.acronym %></td>
        <td><%= image_tag coin.url_image, size: "35x35"%> </td>
        <td><%= link_to t('links.show'), coin %></td>
        <td><%= link_to t('links.edit'), edit_coin_path(coin) %></td>
        <td><%= link_to t('links.delete'), coin, method: :delete, data: { confirm: t('confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('titles.new_coin'), new_coin_path %>
